doctrine_migrations:
    migrations_paths:
        'DoctrineMigrations': '%kernel.project_dir%/migrations'
    enable_profiler: false
    all_or_nothing: true          # Migrations run in transaction (rollback on failure)
    transactional: true
    check_database_platform: true
    organize_migrations: false
